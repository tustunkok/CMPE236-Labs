		ORG	00H
		SJMP	MAIN

		ORG	0BH
		SJMP	T0I

; RESET VECTOR (3-BYTES) 00
; EXTERNAL HW INT 0 (INT0) (8-BYTES) 03
; TIMER 0 INT (8-BYTES) 0B
; EXTERNAL HW INT 1 (8-BYTES) 13
; TIMER 1 INT (8-BYTE) 1B
; SERIAL COM INT (RI AND TI) (8-BYTE) 23

		ORG	30H
MAIN:		SETB	EA
		SETB	ET0
		MOV	TMOD, #01H
		MOV	TH0, #0FFH
		MOV	TL0, #0F0H
		SETB	TR0
		SJMP	$

T0I:		MOV	P1, #00H
		RETI
		END
