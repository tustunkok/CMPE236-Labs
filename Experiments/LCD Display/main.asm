		ORG	00H
		SJMP	MAIN

		ORG	30H
MAIN:		MOV	R2, #38H
		ACALL	CMDWRT
		ACALL	DELAY
	
		MOV	R2, #0EH
		ACALL	CMDWRT
		ACALL	DELAY
	
		MOV	R2, #01H
		ACALL	CMDWRT
		ACALL	DELAY
	
		MOV	R2, #06H
		ACALL	CMDWRT
		ACALL	DELAY
	
		MOV	R2, #84H
		ACALL	CMDWRT
		ACALL	DELAY
	
		MOV	R2, #'H'
		ACALL	DATAWRT
		ACALL	DELAY
	
		MOV	R2, #'E'
		ACALL	DATAWRT
		ACALL	DELAY
	
		MOV	R2, #'L'
		ACALL	DATAWRT
		ACALL	DELAY
	
		MOV	R2, #'L'
		ACALL	DATAWRT
		ACALL	DELAY
	
		MOV	R2, #'O'
		ACALL	DATAWRT
		ACALL	DELAY
	
		SJMP	$

CMDWRT:		MOV	P1, R2
		CLR	P2.0
		CLR	P2.1
		SETB	P2.2
		ACALL	DELAY
		CLR	P2.2
		RET

DATAWRT:	MOV	P1, R2
		SETB	P2.0
		CLR	P2.1
		SETB	P2.2
		ACALL	DELAY
		CLR	P2.2
		RET

DELAY:		MOV	R3, #50
HERE2:		MOV	R4, #255
HERE:		DJNZ	R4, HERE
		DJNZ	R3, HERE2
		RET
		END